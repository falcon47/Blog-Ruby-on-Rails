Añadido Kaminari & Simple_form al proyecto.

Para el Kaminari hemos incluido la gema en el gemfile ( kaminari ) y añadir que
queremos paginar el index cada 2 post.

Para el simple_form hemos igualmente instalado la gema ( simple_form ) y cambiado
el tipo de formularios a simple_form 

